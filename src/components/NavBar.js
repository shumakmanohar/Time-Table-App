import React from "react";

const NavBar = (props) => {
	return (
		<div className="nav-container">
			<div className="nav">
				<h1>Time Table</h1>
				<button>Log out</button>
			</div>
		</div>
	);
};

export default NavBar;
